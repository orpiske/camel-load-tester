# my global config
global:
  scrape_interval: 10s
  evaluation_interval: 30s
  # scrape_timeout is set to the global default (10s).

rule_files:
  - "first.rules"
  - "my/*.rules"

scrape_configs:
  - job_name: test-controller
    metrics_path: /metrics
    static_configs:
      - targets:
# Adjust this
#          - controller-service:9404

  - job_name: test-producer
    metrics_path: /metrics
    static_configs:
      - targets:
# Adjust this
#          - test-producer:9405

  - job_name: test-producer
    metrics_path: /metrics
    static_configs:
      - targets:
# Adjust this
#          - test-producer:9406
